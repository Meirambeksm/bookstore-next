import s from "../page.module.css";
import { FC } from "react";

const Audiobooks: FC = () => {
  return (
    <main className={s.pages}>
      <h1>Audiobooks</h1>
    </main>
  );
};

export default Audiobooks;
