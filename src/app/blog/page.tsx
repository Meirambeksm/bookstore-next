import s from "../page.module.css";
import { FC } from "react";

const Blog: FC = () => {
  return (
    <main className={s.pages}>
      <h1>Blog</h1>
    </main>
  );
};

export default Blog;
